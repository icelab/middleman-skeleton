// Dependencies
import domready from "domready";
import viewloader from "viewloader";

// Components
//import views from "./views";

// Kick off
domready(() => {
  // Execute normal viewloader stuff
  //var manager = viewloader(views);
  //manager.callViews();
});
